COMPILE using the "make" command (from the Makefile)
EXECUTABLE file will then be the "push_swap" file in the bin directory

In the test directory you can find the 42_checker executable for Mac
and the test_generator executable which is a program giving a list of x random numbers (without doubles)
(where x is the argument passed as parameter to test_generator)
